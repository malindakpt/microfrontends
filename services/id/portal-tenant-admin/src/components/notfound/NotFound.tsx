import * as React from "react";

export const NotFound: React.FC = () => {
  const styles = {
    display: "Flex",
    justifyContent: "center"
  };
  return <h1 style={styles}>Page Not Found</h1>;
};
